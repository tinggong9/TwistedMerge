# Broader language and adapter benchmark

The combined ledger contains 5 executed collections across two pinned open BERT bases, including partial full-checkpoint and LoRA collections at ranks 4 and 8. The second-base runs use four real sentiment domains and saved-logit permutation checks. The structured gate was **not passed**; no lift activated.
