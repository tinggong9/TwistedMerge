# Real-image chart inference

Input-inferred D4 charts were evaluated on FashionMNIST, CIFAR10 with three fresh seeds, 500 router examples, 500 selector examples, and 2,000 test examples. The bridge gate was **not passed**. The supplied-chart result is retained only as a diagnostic, not as the primary learned method.
