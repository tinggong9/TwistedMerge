The experiments support controlled chart-aware context handling through Level 2. They do not support a broader claim unless Level 3 passes mechanically. The conservative dispatcher remains essential: ordinary synchronization or a simple baseline is retained whenever the residual gate fails.
