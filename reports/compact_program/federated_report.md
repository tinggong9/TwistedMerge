# Compact federated frame benchmark

The fixed 18-collection grid executed two datasets, three frame regimes, and three seeds with nine matched methods. A persistent residual with a positive lift gain was **not found**. Conditional confirmation was therefore **not triggered**. Exact, noisy, and missing-edge results are all retained.
