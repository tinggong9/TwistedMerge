\begin{tabular}{llllllllll}
\toprule
scope & method & method\_group & intended\_regime & validation\_selected & capacity\_matched & output\_type & independent\_seed\_use & same\_base\_use & claim\_boundary \\
\midrule
regime\_audit & weight\_averaging & generic\_weight\_space & same-base task-vector; same-dataset fine-tune; independent seed as weak/unprotected baseline & no & yes & single averaged model & yes as a deliberately weak unaligned baseline; not a protected alignment method & yes as a basic same-base baseline & Do not present unaligned weight averaging failure on independent random initializations as evidence that task... \\
regime\_audit & greedy\_soup\_model\_soups & validation\_selected\_soup & same-base task-vector; same-dataset fine-tune; checkpoint soup over a common candidate pool & yes & yes & single averaged soup & yes as an empirical validation-descent baseline over the current candidate chec... & yes as a canonical same-base fine-tuned checkpoint-soup baseline & Do not claim TwistedMerge beats Model Soups broadly unless the exact official/same-base soup setup and paired... \\
regime\_audit & slerp & same\_base\_interpolation & same-base task-vector; same-dataset fine-tune; interpolation between related checkpoints & usually no unless mixing coefficient is selected & yes & single interpolated model & no; independent random initializations make spherical interpolation a geometry... & yes & Do not judge SLERP from independent random-initialization model merging without a same-base task-vector setup. \\
regime\_audit & task\_arithmetic & same\_base\_task\_vector & same-base task-vector; multi-task fine-tuning from one pretrained/base model & optional for coefficients & yes & single task-vector-composed model & no; current fixed-setting rows are independently trained seeds and not task vec... & yes & Do not claim Task Arithmetic underperforms or fails from the current independent-seed benchmark series 11 fixed-setting... \\
regime\_audit & ties\_merging & same\_base\_task\_vector & same-base task-vector; multi-task fine-tuning from one pretrained/base model & optional for density/merge hyperparameters & yes & single merged model after sign/elect/disjoint merge & no; independent random initializations violate the shared task-vector assumption & yes & Do not use independent random-initialization MLP merging to claim TIES is weak or unfair. \\
regime\_audit & dare & same\_base\_task\_vector & same-base task-vector; dropout/rescale of fine-tuned task deltas from a common base & optional for drop rate/rescale choices & yes & single merged model or merged task-vector model & no; current fixed-setting data lack common-base deltas & yes & Do not judge DARE from independent random-initialization model merging without constructing same-base task ve... \\
regime\_audit & git\_rebasin\_pairwise & permutation\_rebasin & independent seed; same architecture; same task/dataset; rebasin before averaging & no & yes & single aligned average & yes as a faithful Git-ReBasin-style internal baseline; not as official-code evi... & partly as an alignment diagnostic but less central if common base already align... & Do not claim an official Git Re-Basin win unless official code is run on the exact benchmark. \\
regime\_audit & c2m3\_synchronization & cycle\_consistent\_rebasin & independent seed; same architecture; same task/dataset; pairwise alignments with cycle consist... & no & yes & single synchronized aligned average & yes as the internal C2M3-style reference for current fixed-setting claims; not... & partly as a stress test but not the primary same-base task-vector baseline & Do not claim TwistedMerge beats official C2M3 from faithful internal C2M3-style rows alone. \\
regime\_audit & monomial\_gauge\_alignment & exact\_relu\_gauge & independent seed ReLU networks with compatible positive scaling/permutation gauges & optional for shrinkage/global scale selection & yes & single same-capacity aligned average & yes as an internal exact ReLU-gauge diagnostic/baseline & partly but secondary unless the same-base benchmark uses ReLU architectures wit... & Do not present monomial gauges as Task Arithmetic/TIES/DARE competitors in the same-base task-vector sense. \\
regime\_audit & twisted\_rank\_lift\_branch\_candidates & controlled\_obstruction\_lift & controlled obstruction; central twist; optional real-model diagnostic only & yes for branch/context selection or validation controls & no & branch/rank-lift model or branch ensemble & limited; only as extra-capacity/control diagnostics & limited; only if branch capacity and task-vector baselines are separately match... & Do not compare branch/rank-lift results directly to same-capacity SLERP/Task Arithmetic/TIES/DARE as if all w... \\
\bottomrule
\end{tabular}
