| scope | method | method_group | intended_regime | validation_selected | capacity_matched | output_type | independent_seed_use | same_base_use | claim_boundary |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| regime_audit | weight_averaging | generic_weight_space | same-base task-vector; same-dataset fine-tune; independent seed as weak/unprotected baseline | no | yes | single averaged model | yes as a deliberately weak unaligned baseline; not a protected alignment method | yes as a basic same-base baseline | Do not present unaligned weight averaging failure on independent random initializations as evidence that task-vector me... |
| regime_audit | greedy_soup_model_soups | validation_selected_soup | same-base task-vector; same-dataset fine-tune; checkpoint soup over a common candidate pool | yes | yes | single averaged soup | yes as an empirical validation-descent baseline over the current candidate chec... | yes as a canonical same-base fine-tuned checkpoint-soup baseline | Do not claim TwistedMerge beats Model Soups broadly unless the exact official/same-base soup setup and paired statistic... |
| regime_audit | slerp | same_base_interpolation | same-base task-vector; same-dataset fine-tune; interpolation between related checkpoints | usually no unless mixing coefficient is selected | yes | single interpolated model | no; independent random initializations make spherical interpolation a geometry... | yes | Do not judge SLERP from independent random-initialization model merging without a same-base task-vector setup. |
| regime_audit | task_arithmetic | same_base_task_vector | same-base task-vector; multi-task fine-tuning from one pretrained/base model | optional for coefficients | yes | single task-vector-composed model | no; current fixed-setting rows are independently trained seeds and not task vec... | yes | Do not claim Task Arithmetic underperforms or fails from the current independent-seed fixed-setting fixed-setting data. |
| regime_audit | ties_merging | same_base_task_vector | same-base task-vector; multi-task fine-tuning from one pretrained/base model | optional for density/merge hyperparameters | yes | single merged model after sign/elect/disjoint merge | no; independent random initializations violate the shared task-vector assumption | yes | Do not use independent random-initialization MLP merging to claim TIES is weak or unfair. |
| regime_audit | dare | same_base_task_vector | same-base task-vector; dropout/rescale of fine-tuned task deltas from a common base | optional for drop rate/rescale choices | yes | single merged model or merged task-vector model | no; current fixed-setting data lack common-base deltas | yes | Do not judge DARE from independent random-initialization model merging without constructing same-base task vectors. |
| regime_audit | git_rebasin_pairwise | permutation_rebasin | independent seed; same architecture; same task/dataset; rebasin before averaging | no | yes | single aligned average | yes as a faithful Git-ReBasin-style internal baseline; not as official-code evi... | partly as an alignment diagnostic but less central if common base already align... | Do not claim an official Git Re-Basin win unless official code is run on the exact benchmark. |
| regime_audit | c2m3_synchronization | cycle_consistent_rebasin | independent seed; same architecture; same task/dataset; pairwise alignments with cycle consist... | no | yes | single synchronized aligned average | yes as the internal C2M3-style reference for current fixed-setting claims; not... | partly as a stress test but not the primary same-base task-vector baseline | Do not claim TwistedMerge beats official C2M3 from faithful internal C2M3-style rows alone. |
| regime_audit | monomial_gauge_alignment | exact_relu_gauge | independent seed ReLU networks with compatible positive scaling/permutation gauges | optional for shrinkage/global scale selection | yes | single same-capacity aligned average | yes as an internal exact ReLU-gauge diagnostic/baseline | partly but secondary unless the same-base benchmark uses ReLU architectures wit... | Do not present monomial gauges as Task Arithmetic/TIES/DARE competitors in the same-base task-vector sense. |
| regime_audit | twisted_rank_lift_branch_candidates | controlled_obstruction_lift | controlled obstruction; central twist; optional real-model diagnostic only | yes for branch/context selection or validation controls | no | branch/rank-lift model or branch ensemble | limited; only as extra-capacity/control diagnostics | limited; only if branch capacity and task-vector baselines are separately match... | Do not compare branch/rank-lift results directly to same-capacity SLERP/Task Arithmetic/TIES/DARE as if all were single... |
